// Copy this to `ecosystem.config.js` (gitignored — paths/tuning are per-machine).
module.exports = {
  apps: [{
    name: 'shop-api',
    script: './dist/main.js',
    cwd: __dirname,
    instances: 1,          // Cluster mode: tận dụng tất cả CPU cores
    exec_mode: 'cluster',      // QUAN TRỌNG: NestJS hỗ trợ cluster
    autorestart: true,
    watch: false,               // TẮT watch trên production
    max_memory_restart: '512M',   // Auto restart nếu rò rỉ bộ nhớ
    env_production: {
      NODE_ENV: 'production',
      PORT: 3000,
    },
    // Logs — omit error_file/out_file to use PM2's default ~/.pm2/logs/*.
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
  }]
}
